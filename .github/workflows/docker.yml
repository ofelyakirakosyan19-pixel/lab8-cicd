name: Docker CI Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Build custom Docker image
      run:
        docker build -t ofelya-app:latest .
        docker build -t ofelya-app:${{ github.sha }} .
